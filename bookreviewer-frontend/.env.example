# Relative base URL — Vite proxies /api to the Spring backend (see vite.config.js).
# Leave as /api for local Vite + Docker nginx. Only set an absolute URL if the
# API is on another origin and CORS is configured for that frontend origin.
VITE_API_BASE_URL=/api
